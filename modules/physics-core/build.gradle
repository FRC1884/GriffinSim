plugins {
    id 'java-library'
}

dependencies {
    api project(':modules:sim-contracts')
    implementation project(':modules:sensor-emulation')
    testImplementation "org.junit.jupiter:junit-jupiter:${rootProject.ext.junitVersion}"
}
